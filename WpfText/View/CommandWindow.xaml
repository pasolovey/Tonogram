<Window x:Class="WpfText.View.CommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfText.View"
        mc:Ignorable="d" Height="75" Width="455" 
        SizeToContent="WidthAndHeight"
        WindowStyle="None" 
        ResizeMode="NoResize">
   <Window.Resources>

      <Style x:Key="MyFocusVisual">
         <Setter Property="Control.Template">
            <Setter.Value>
               <ControlTemplate>
                  <Rectangle Margin="-2" StrokeThickness="1" Stroke="Red" StrokeDashArray="1 2"/>
               </ControlTemplate>
            </Setter.Value>
         </Setter>
      </Style>

      <!--<Style TargetType="{x:Type Button}">
         <Setter Property="FocusVisualStyle" Value="{StaticResource MyFocusVisual}" />
      </Style>-->

   </Window.Resources>
   <Grid>
      <WrapPanel Name ="Stack"  Orientation="Horizontal" Width="500" />
      <!--<StackPanel Name="Stack" Orientation ="Horizontal">
      </StackPanel>-->
   </Grid>
</Window>
